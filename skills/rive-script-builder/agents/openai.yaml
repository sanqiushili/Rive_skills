interface:
  display_name: "Rive Script Builder v2 · 三秋十李Sergio"
  short_description: "RiveCN.com official scripting skill"
  default_prompt: "Use $rive-script-builder to choose the right Rive protocol, clarify only implementation-changing uncertainty, and generate the script directly when the request is clear; otherwise present a pending plan and wait for approval."
